### Security

- Validate each metadata collection's snapshot resource version independently while requiring the exact reduced Job and Pod identities to match across both observations. Repeat that current-state barrier after selector authority is atomically quarantined, confirm the named RoleBinding is inert, and prove the exact selector authorization denial before the source compare-and-swap.
