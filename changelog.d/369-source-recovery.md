### Fixed

- Bind the platform publisher to the complete successful main-CI step inventory and add a finite, source-bound recovery workflow that preserves original CI, actual executor identity, immutable predecessors and ordinary-token publication limits.
