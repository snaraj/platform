### Changed

- Advance the lockstep CodeQL `init` and `analyze` workflow pins from 4.38.0 to 4.38.1 at the exact commit independently peeled from the annotated upstream tag.
- Supersede Dependabot PR #386 with the signed, issue-linked release fragment and publication history the protected delivery lane requires.
